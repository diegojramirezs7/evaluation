{
  "name": "proto",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "axios": "^0.19.2",
    "google-map-react": "^1.1.7",
    "google-maps-react": "^2.0.6",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:prod": "docker build -t diegojramirezs7/weather .",
    "deploy:prod": "npm run clean && npm run compile",
    "start:prod": "docker run --rm -d -p 80:80 --name bc-weather diegojramirezs7/weather",
    "create:aws": "chmod +x ./bin/deploy.sh; ./bin/deploy.sh create",
    "deploy:aws": "chmod +x ./bin/deploy.sh; ./bin/deploy.sh deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
